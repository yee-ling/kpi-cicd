# Pinned dependencies for the container image (pip, not conda).
# Ranges kept compatible with lesson_03's environment.yml.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
mlflow>=3.1
scikit-learn>=1.3
xgboost>=2.0
pandas>=2.0
numpy>=1.24